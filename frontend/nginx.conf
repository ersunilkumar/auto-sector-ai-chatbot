server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri /index.html;
    }

    location /chat {
        proxy_pass http://auto-backend:8000/chat;
        proxy_set_header Host $host;
    }
}
